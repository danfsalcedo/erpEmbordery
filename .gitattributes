# Normalizar finales de línea: LF en el repositorio (los contenedores son Linux).
* text=auto eol=lf

# Scripts de shell SIEMPRE en LF: CRLF rompe su ejecución en contenedores Linux.
*.sh text eol=lf
